mod basetype;
mod compound;
mod flow;
mod generic;
mod matchpattern;
mod method;
mod ownership;
mod traits;
mod variable;

fn main() {
    // variable::_var();
    // basetype::_base();
    // ownership::_entry();
    // compound::_entry();
    // flow::_entry();
    // matchpattern::_entry();
    // method::_entry();
    // generic::_entry();

    // 特征
    traits::_entry();
}
