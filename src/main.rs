mod advancetrait;
mod basetype;
mod compound;
mod flow;
mod generic;
mod matchpattern;
mod method;
mod ownership;
mod traitobj;
mod traits;
mod variable;

fn main() {
    // variable::_var();
    // basetype::_base();
    // ownership::_entry();
    // compound::_entry();
    // flow::_entry();
    // matchpattern::_entry();
    // method::_entry();
    // generic::_entry();

    // 特征
    // traits::_entry();

    // 特征对象
    // traitobj::_entry();

    // 深入特征
    advancetrait::_entry();
}
